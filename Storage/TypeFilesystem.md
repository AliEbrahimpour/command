# 📘 انواع TYPE در خروجی `lsblk`

| TYPE                                  | توضیح                                 | مثال در خروجی       | کاربرد معمول                                        |
| ------------------------------------- | ------------------------------------- | ------------------- | --------------------------------------------------- |
| **disk**                              | دیسک فیزیکی (HDD, SSD, NVMe)          | `nvme0n1`, `sda`    | دیسک اصلی سیستم، شامل پارتیشن‌ها                    |
| **part**                              | پارتیشن روی یک دیسک                   | `nvme0n1p1`, `sda2` | تقسیم‌بندی منطقی دیسک فیزیکی                        |
| **lvm**                               | Logical Volume در LVM                 | `vg0-os`, `vg0-lv1` | برای مدیریت پویا و flexible فضای ذخیره‌سازی         |
| **raid**                              | دستگاه RAID (مثل mdadm)               | `md0`, `md127`      | برای Redundancy یا افزایش performance               |
| **rom**                               | دستگاه فقط خواندنی (Read-Only Media)  | `sr0`               | معمولاً CD/DVD یا ISO mount شده                     |
| **loop**                              | فایل mount شده به‌صورت دیسک مجازی     | `loop0`, `loop1`    | برای snap packageها، تست یا mount فایل‌های ISO      |
| **crypt**                             | دستگاه رمزگذاری‌شده (LUKS)            | `cryptroot`         | برای امنیت داده‌ها با encryption                    |
| **dm**                                | Device Mapper (مثل LVM یا cryptsetup) | `dm-0`, `dm-1`      | سطح انتزاعی بین فایل‌سیستم و سخت‌افزار              |
| **nbd**                               | Network Block Device                  | `nbd0`, `nbd1`      | دیسک مجازی از طریق شبکه (برای cloud یا image mount) |
| **mpath**                             | Multipath Device                      | `mpatha`, `mpathb`  | برای مسیرهای چندگانه به storage (SAN، iSCSI)        |
| **zram**                              | فشرده‌سازی رم به‌صورت swap یا tmpfs   | `zram0`             | برای افزایش فضای مجازی رم                           |
| **swap** *(در `lsblk -f` دیده می‌شه)* | فضای swap سیستم                       | —                   | برای مدیریت حافظه مجازی                             |

---

## 🧠 نکات فنی برای درک بهتر:

* ستون **`TYPE`** ساختار درختی دستگاه‌ها رو مشخص می‌کنه. مثلاً:

  ```
  nvme0n1 (disk)
  ├─nvme0n1p1 (part)
  └─nvme0n1p2 (part)
     ├─vg0-os (lvm)
     └─vg0-lv1 (lvm)
  ```
